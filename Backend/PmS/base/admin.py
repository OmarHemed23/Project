from django.contrib import admin
from base.models import Address,PropertyType,Property

# Register your models here.
admin.site.register(Address)
admin.site.register(PropertyType)
admin.site.register(Property)
